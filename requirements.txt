# Full environment (works on the GPU cluster and on macOS).
# On the cluster, install torch from the official index for your CUDA build.
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
torch>=2.2
torchvision>=0.17
timm>=1.0
matplotlib>=3.8
streamlit>=1.33
wandb>=0.16
pytest>=8.0
ruff>=0.4
